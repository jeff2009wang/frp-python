# 🚀 快速开始指南

## 📋 部署前检查清单

在开始部署前，请确认以下条件：

### 1. 服务器信息
- [x] 客户端服务器: 47.117.159.145:9321
- [x] 服务端服务器: 8.162.10.216:22
- [x] 两台服务器的root密码已确认

### 2. 网络环境
- [x] 本地机器可以SSH连接到两台服务器
- [x] 服务器的防火墙/安全组已配置（或将在部署后配置）
- [x] 两台服务器之间网络互通

### 3. 本地环境
- [x] Python 3.7+已安装
- [x] 可以访问GitHub（下载依赖）

---

## ⚡ 一键部署（最简单）

### Windows PowerShell

```powershell
# 进入部署目录
cd deploy

# 安装依赖
pip install paramiko cryptography

# 一键部署（交互式）
python quick_deploy.py
```

### Linux/Mac Terminal

```bash
# 进入部署目录
cd deploy

# 安装依赖
pip3 install paramiko cryptography

# 一键部署（交互式）
bash quick_deploy.sh
```

---

## 📝 部署步骤

### 方式1: 使用一键脚本（推荐）

```bash
# 运行一键部署脚本
bash quick_deploy.sh
```

脚本会引导您：
1. 选择部署协议（Hysteria2 或 Python QUIC）
2. 确认服务器信息
3. 配置服务端口
4. 自动执行部署
5. 自动验证结果

### 方式2: 分步部署

#### 步骤1: 测试服务器连接

```bash
python ssh_manager.py
```

应该看到：
```
✓ 客户端服务器连接成功
✓ 服务端服务器连接成功
```

#### 步骤2: 执行部署

**部署Hysteria2（推荐）**:
```bash
python auto_deploy.py --protocol hysteria2 --server-port 4433
```

**部署Python QUIC**:
```bash
python auto_deploy.py --protocol quic --server-port 7000
```

#### 步骤3: 验证部署

```bash
python verify_deployment.py --protocol hysteria2
```

---

## 🎯 协议选择建议

### Hysteria2（推荐用于生产环境）

**优势**:
- ✅ 性能极高（80-100 MB/s）
- ✅ 弱网表现优秀
- ✅ 稳定可靠

**使用场景**:
- 生产环境
- 需要高性能
- 网络质量一般

**部署命令**:
```bash
python auto_deploy.py --protocol hysteria2
```

### Python QUIC（推荐用于开发测试）

**优势**:
- ✅ 纯Python实现
- ✅ 易于定制
- ✅ 便于调试

**使用场景**:
- 开发测试
- 需要定制功能
- 学习研究

**部署命令**:
```bash
python auto_deploy.py --protocol quic
```

---

## ✅ 部署验证

### 自动验证

```bash
python verify_deployment.py --protocol hysteria2
```

### 手动验证

#### 1. 检查服务状态

```bash
# SSH到客户端服务器
ssh -p 9321 root@47.117.159.145

# 检查Hysteria2客户端
systemctl status hysteria2-client

# 检查Python QUIC
systemctl status frp-quic
```

#### 2. 测试SOCKS5代理（仅Hysteria2）

```bash
# 在客户端服务器上
curl -x socks5://127.0.0.1:1080 http://www.baidu.com -I
```

应该返回：
```
HTTP/1.1 200 OK
...
```

#### 3. 查看日志

```bash
# Hysteria2
journalctl -u hysteria2-server -f
journalctl -u hysteria2-client -f

# Python QUIC
journalctl -u frp-quic -f
```

---

## 📊 预期结果

### Hysteria2部署

```
服务端服务器:
  - 服务状态: ✓ 运行中
  - 监听端口: 4433 (UDP/TCP)
  - 认证密码: (自动生成)

客户端服务器:
  - 服务状态: ✓ 运行中
  - SOCKS5端口: 1080
  - 代理地址: 47.117.159.145:1080

网络连通性:
  - 客户端→服务端: ✓ 通
  - SOCKS5代理: ✓ 工作正常
```

### Python QUIC部署

```
服务端服务器:
  - 服务状态: ✓ 运行中
  - 监听端口: 7000 (UDP/TCP)
  
客户端:
  - 需要运行frpc_quic.py连接
```

---

## 🔧 常见问题

### Q1: SSH连接失败

```bash
# 测试SSH连接
ssh -p 9321 root@47.117.159.145

# 检查:
# 1. IP地址是否正确
# 2. 端口是否正确
# 3. 密码是否正确
```

### Q2: 部署失败

```bash
# 查看详细日志
python auto_deploy.py --protocol hysteria2 2>&1 | tee deploy.log
```

### Q3: 服务无法启动

```bash
# SSH到服务器
ssh -p 22 root@8.162.10.216

# 查看服务状态
systemctl status hysteria2-server

# 查看日志
journalctl -u hysteriaia2-server -n 50
```

### Q4: 无法连接代理

```bash
# 检查端口
ss -tuln | grep 1080

# 重启客户端
systemctl restart hysteria2-client

# 检查配置
cat /etc/hysteria2/client.yaml
```

---

## 📞 获取帮助

### 查看详细文档

```bash
cat README.md
```

### 查看故障排查

```bash
# 打开README.md，查看"故障排查"部分
```

### 提交问题

如果遇到问题，请提供：
1. 错误信息截图
2. 部署日志
3. 服务状态
4. 系统环境

---

## 📁 文件说明

```
deploy/
├── START_HERE.md           # 📖 本文件，快速开始指南
├── README.md               # 📚 完整部署文档
├── ssh_manager.py          # 🔌 SSH连接管理
├── auto_deploy.py          # 🚀 自动化部署脚本
├── verify_deployment.py    # ✅ 验证测试脚本
├── hysteria2_installer.sh  # 📦 Hysteria2安装脚本
├── quick_deploy.sh         # ⚡ 一键部署脚本（Linux/Mac）
├── quick_deploy.py         # ⚡ 一键部署脚本（Windows）
└── requirements.txt        # 📋 Python依赖
```

---

## 🎉 开始部署

准备好了吗？让我们开始：

```bash
cd deploy
bash quick_deploy.sh
```

**祝您部署顺利！** 🚀
